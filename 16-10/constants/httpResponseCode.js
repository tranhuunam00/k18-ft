// tạo nhưng thành phần const cho http code , error message
const STATUS_CODE = {
  success: 200,
  created: 201,
  badRequest: 400,
  notFounded: 404,
  errorServer: 500,
};

module.exports = STATUS_CODE;
